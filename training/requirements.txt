# Core training
torch>=2.3.0
onnx>=1.16.0
onnxruntime>=1.18.0        # CPU export verification; swap for onnxruntime-gpu on CUDA hosts
onnxsim>=0.3.8             # ONNX graph simplification (used by export.py; optional but recommended)
scipy>=1.13.0
numpy>=1.26.0

# TPU (Google Colab v5e — install separately, version must match torch)
# pip install torch_xla[tpu] -f https://storage.googleapis.com/libtpu-releases/index.html

# Data loading & augmentation
opencv-python>=4.10.0
Pillow>=10.3.0
albumentations>=1.4.0
pycocotools>=2.0.8

# Utilities
PyYAML>=6.0.1
tqdm>=4.66.0
tensorboard>=2.17.0
